#!/usr/bin/env bash
set -euo pipefail

printf 'SSH' >>"${MIA_OUTAGE_TEST_LOG:?}"
printf ' %q' "$@" >>"${MIA_OUTAGE_TEST_LOG}"
printf '\n' >>"${MIA_OUTAGE_TEST_LOG}"
echo "fixture worker DNS/SSH outage" >&2
exit "${MIA_OUTAGE_SSH_STATUS:-255}"
